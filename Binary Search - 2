1. Find square root of a number in log n

1. Find square root of a number in log n
n = int(input())
low = 1
high = n
while(low<=high):
    mid = (low+high)//2
    val = mid*mid
    if val<=n:
        low = mid+1
    else:
        high = mid-1
print(high)
